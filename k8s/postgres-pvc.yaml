apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anime-backlog-postgres-data
  namespace: anime-backlog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
